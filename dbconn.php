<?php
/**
 * dbconn.php — TEMPLATE. Copy this to a real dbconn.php with your
 * actual database credentials filled in, and put that copy in one of
 * the locations config/config.php checks (see the comment there) —
 * ideally OUTSIDE your web root entirely. Never commit or upload the
 * real, filled-in file anywhere this template itself gets shared
 * (git, zips you send around, etc.) — see the notes sent alongside
 * this file for exactly where to place it and why.
 *
 * Must define exactly these four variables and produce no other
 * output:
 */
$servernm = 'localhost';           // DB host — usually "localhost" on shared/cPanel hosting
$dbname   = 'atest8a6_inventorydb';
$username = 'atest8a6_inv001';
$pwd      = 'D({7uSLiEuW0.UnK';
